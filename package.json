{
  "name": "art-institut-stack",
  "private": true,
  "version": "0.0.1",
  "scripts": {
    "prepare": "husky install",
    "lint:commits": "commitlint --from=origin/main --to=HEAD"
  },
  "devDependencies": {
    "@commitlint/cli": "^18.6.1",
    "@commitlint/config-conventional": "^18.6.3",
    "husky": "^9.1.6"
  }
}
